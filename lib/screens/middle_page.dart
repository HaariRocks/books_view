import 'package:flutter/material.dart';

class MiddlePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("MiddlePage")),
    );
  }
}
